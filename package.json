{
  "name": "house-of-tartufo-price-list",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:brand": "node scripts/prepare-brand-assets.mjs",
    "dev": "npm run prepare:brand && npm run sync:catalog && vite",
    "build": "npm run prepare:brand && npm run sync:catalog && vite build",
    "typecheck": "tsc --noEmit",
    "sync:catalog": "node scripts/sync-catalog.mjs",
    "test": "vitest run tests",
    "test:watch": "vitest tests",
    "qa:preview": "node scripts/qa-preview.mjs",
    "qa:browser": "playwright test",
    "build:release": "node scripts/create-release-candidate.mjs",
    "audit:shopify": "node scripts/audit-shopify-catalog.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@types/node": "^22.17.0",
    "typescript": "^5.9.2",
    "vite": "^7.0.6",
    "vitest": "^3.2.4"
  }
}
